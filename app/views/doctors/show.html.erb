<h2><%= @doctor.name %></h2>
<p><%= @doctor.department %></p>

<ul>
	<% @doctor.appointments.each do |appt| %>
		<li><%= appt.appointment_in_human_time%> 
			<%= link_to appt.patient.name, patient_path(appt.patient)%></li>
	<% end %>
</ul>
